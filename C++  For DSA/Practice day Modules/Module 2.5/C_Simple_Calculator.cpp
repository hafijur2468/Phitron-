#include <bits/stdc++.h>

int main()
{
    long long int a, b;
    std::cin >> a >> b;
    printf("%lld + %lld = %lld\n", a, b, a + b);
    printf("%lld * %lld = %lld\n", a, b, a * b);
    printf("%lld - %lld = %lld\n", a, b, a - b);

    return 0;
}